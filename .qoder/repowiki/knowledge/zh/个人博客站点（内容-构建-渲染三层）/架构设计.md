仓库按职责拆分为三个子模块：posts_content 维护 posts/ 下的 Markdown 原文与碎片；build_tools 扫描这些原文并生成 site_pages/data/ 中的 JSON/JS 数据文件（含全文索引），同时提供 watcher 实现增量重建；site_pages 作为纯前端层，在浏览器中动态加载 data/*.js 完成首页、文章、碎片的渲染。三者以 data/ 目录为契约边界——构建产物是页面层的唯一数据源，页面层不直接读取 posts/，形成单向依赖链 posts_content → build_tools → site_pages。